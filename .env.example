# Copy to .env to enable the AI Studio tab's question-generation feature.
# .env is gitignored — the key never leaves your machine and is only read
# by scripts/serve.js (npm run dev / npm run serve), never sent to the browser.

ANTHROPIC_API_KEY=

# Optional — defaults to claude-opus-4-8
# ANTHROPIC_MODEL=claude-opus-4-8
